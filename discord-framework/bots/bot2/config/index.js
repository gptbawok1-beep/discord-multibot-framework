/**
 * Bot 2 Configuration
 *
 * Reads environment variables via the shared config loader.
 */

import config from '../../../shared/config/index.js';

/** @type {BotConfig} */
const bot2Config = config.bot2;

export default bot2Config;
